type Query {
  careers: [Career]
}

type Career {
  id: ID!
  title: String!
  location: String!
  appLink: String
}

type NewsArticle {
  id: ID!
  title: String!
  description: String
  articleLink: String
}
